name: Scrape Property Finder

on:
  schedule:
    - cron: "0 */6 * * *"   # every 6 hours: 00:00, 06:00, 12:00, 18:00 UTC
  workflow_dispatch:          # manual trigger from GitHub UI

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Wake up API
        run: |
          echo "Pinging API at $(date)"
          curl -s "${{ secrets.API_URL }}/health" || true
          sleep 10

      - name: Trigger scrape
        run: |
          echo "Starting scrape at $(date)"
          curl -s -X POST \
            "${{ secrets.API_URL }}/api/trigger-scrape?pages=300&rental_pages=200&secret=${{ secrets.SCRAPE_SECRET }}" \
            --max-time 21600 \
            -w "\nHTTP Status: %{http_code}\nTime: %{time_total}s"
          echo "Done at $(date)"
